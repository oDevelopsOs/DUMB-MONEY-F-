[build]
  command = "npm run build"
  publish = "dist"

# Ensure API routes are resolved by Astro SSR function on Netlify.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/entry/api/:splat"
  status = 200
  force = true
